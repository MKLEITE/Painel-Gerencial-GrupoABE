{
  "name": "@abe/canonical-model",
  "version": "0.1.0",
  "private": true,
  "description": "Modelo canônico compartilhado (tipos/enums) entre backend, workers e frontend",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@abe/config": "workspace:*",
    "eslint": "^9.11.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  }
}
