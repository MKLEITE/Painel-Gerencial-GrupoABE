{
  "name": "@abe/workers",
  "version": "0.1.0",
  "private": true,
  "description": "Workers de integração (sincronização das fontes para a réplica)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@abe/canonical-model": "workspace:*"
  },
  "devDependencies": {
    "@abe/config": "workspace:*",
    "@types/node": "^22.7.4",
    "eslint": "^9.11.1",
    "typescript": "^5.6.2"
  }
}
